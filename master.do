* =============================================================================
* Master Do File: 四大审计对ESG评级质量的影响
* 论文：外部审计治理与ESG评级质量——基于四大会计师事务所审计的准自然实验
* 作者：[作者姓名]
* 数据：2013-2022年中国A股上市公司
* 使用方法：Stata中运行 do master.do
* =============================================================================

clear all
set more off
capture log close

* 设置工作路径（请根据实际路径修改）
global ROOT  "."
global DATA  "$ROOT/data"
global CODE  "$ROOT/code"
global OUTPUT "$ROOT/output"

* 创建输出目录
capture mkdir "$DATA"
capture mkdir "$OUTPUT"

* 设置随机种子确保可复现
set seed 20260722

* 安装所需Stata包（首次运行时取消注释）
* ssc install winsor2
* ssc install estout
* ssc install coefplot
* ssc install psmatch2
* ssc install outreg2
* ssc install esttab

display "========================================"
display "  开始运行..."
display "========================================"

* =============================================================================
* 第一步：生成模拟数据
* 说明：仅生成模拟数据用于代码验证，不包含任何真实企业信息
* =============================================================================
do "$CODE/data_generation.do"

* =============================================================================
* 第二步：维度一——四大审计与ESG评级分歧度
* =============================================================================
do "$CODE/dimension1_divergence.do"

* =============================================================================
* 第三步：维度二——四大审计与ESG评级预测力
* =============================================================================
do "$CODE/dimension2_predictive.do"

* =============================================================================
* 第四步：维度三——四大审计与ESG评级颗粒度
* =============================================================================
do "$CODE/dimension3_granularity.do"

* =============================================================================
* 第五步：平行趋势检验
* =============================================================================
do "$CODE/parallel_trend.do"

* =============================================================================
* 第六步：异质性分析
* =============================================================================
do "$CODE/heterogeneity.do"

* =============================================================================
* 第七步：稳健性检验
* =============================================================================
do "$CODE/robustness.do"

* =============================================================================
* 完成
* =============================================================================
display "========================================"
display "  所有分析完成！"
display "  结果文件保存在: $OUTPUT"
display "  请查看以下文件："
display "    - $OUTPUT/维度一_基准回归.csv"
display "    - $OUTPUT/维度二_基准回归.csv"
display "    - $OUTPUT/维度三_基准回归.csv"
display "    - $OUTPUT/异质性_维度二.csv"
display "    - $OUTPUT/异质性_维度三.csv"
display "    - $OUTPUT/稳健性_PSM_DID.csv"
display "    - $OUTPUT/图1_平行趋势_分歧度.png"
display "    - $OUTPUT/图2_事件研究_颗粒度.png"
display "    - $OUTPUT/安慰剂检验_分歧度.png"
display "========================================"
